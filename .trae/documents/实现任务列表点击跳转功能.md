1. **修改 TaskListPage.tsx**：
   - 为任务列表中的 `tr` 元素添加 `onClick` 事件处理函数，使其跳转到任务详情页面
   - 确保任务行中的其他按钮点击事件不会触发整个任务行的跳转

2. **修改内容**：
   - 在 `tr` 元素上添加 `onClick={() => navigate(`/task/${task.id}`)}`
   - 为操作按钮添加 `e.stopPropagation()` 以阻止事件冒泡
   - 保持现有功能不变，只是扩展点击区域

3. **验证**：
   - 点击任务行的任何位置（除了操作按钮）都应该跳转到任务详情页面
   - 操作按钮的功能保持不变

这个修改将提高用户体验，使用户可以更方便地查看任务详情。